{
  "buildCommand": "cd site && npm install && npm run build",
  "outputDirectory": "site/.next",
  "installCommand": "cd site && npm install",
  "devCommand": "cd site && npm run dev --port $PORT",
  "framework": "nextjs"
}
